# Assumes that it will connect to the test application deployed by "./gradlew -i mlDeploy"
config.username=admin
config.password=admin
config.hostName=localhost

# Uses a test keystore, same as the one found in the marklogic/nifi repository for testing certificate authentication
keystore.filepath=src/test/resources/test-keystore.jks
keystore.password=passwordpassword